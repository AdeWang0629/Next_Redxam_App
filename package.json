{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "client": "cd packages/frontend && yarn dev",
    "server": "cd packages/backend && yarn start",
    "start": "concurrently \"yarn run client\" \"yarn run server\""
  },
  "devDependencies": {
    "concurrently": "^7.0.0",
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "stripe": "^8.201.0"
  }
}
